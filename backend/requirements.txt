fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
asyncpg==0.30.0
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 breaks against bcrypt 5.x (its internal self-test raises on the
# 72-byte limit), so the backend's password hashing must stay on bcrypt 4.x.
bcrypt==4.0.1
python-multipart==0.0.12
email-validator==2.2.0
